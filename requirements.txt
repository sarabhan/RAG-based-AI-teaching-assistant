# Core
numpy>=1.24.0
pandas>=2.1.0

# Sentence Transformers for embeddings
sentence-transformers>=2.2.2
torch>=2.1.0

# MongoDB client
pymongo>=4.4.0

# Audio / Video processing (if using ffmpeg / audio extraction)
pydub>=0.25.1
ffmpeg-python>=0.0.25

# Ollama client for LLaMA model integration
ollama>=0.0.20

# Optional: tqdm for progress bars
tqdm>=4.66.0
